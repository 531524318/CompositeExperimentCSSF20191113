<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="cb_test">
        <!--选中的颜色-->
        <item name="colorAccent">@color/blue_deep</item>
    </style>
    <!-- EditView 常用样式 -->
    <style name="CustomEditViewStyle">
        <item name="android:textCursorDrawable">@drawable/custor_shape1</item>
        <item name="android:drawableRight">@drawable/clean_bg_selector</item>
        <item name="android:textSize">@dimen/textsize_16</item>
        <item name="android:textColor">#1e1e1e</item>
        <item name="android:textColorHint">@color/gray_font</item>
        <item name="android:singleLine">true</item>
        <item name="android:scrollHorizontally">true</item>
    </style>
    <style name="CustomButtonGreenStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:layout_marginLeft">10dp</item>
        <item name="android:layout_marginRight">10dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/textsize_16</item>
        <item name="android:background">@drawable/btn_green_selector_style</item>
    </style>
    <style name="MyCheckBox" parent="@android:style/Widget.CompoundButton.CheckBox">
        <item name="android:button">@drawable/checkbox_style</item>
    </style>
</resources>
